export const BASE_API_URL = `http://${location.hostname}:8080/api/v1`;
export const BASE_WS_URL = `ws://${location.hostname}:8765`;

export const SENSOR_POLLING_INTERVAL_MS = 60 * 1000; // 1 minute
export const SENSOR_AGGREGATION_INTERVAL_MS = 60 * 60 * 1000; // 1 hour
